apiVersion: v1
kind: Service
metadata:
  name: orders
  namespace: shop
  labels:
    app: orders
spec:
  type: ClusterIP
  selector:
    app: orders
  ports:
  - name: http
    port: 8083
    targetPort: 8083
